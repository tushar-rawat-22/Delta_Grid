# Projection boundary

This directory independently validates the exact public P1.1 projection/manifest shape before any future package can be admitted to the web build.

P1.2 implements and tests the verifier but does not ship a current package. P1.3 is the explicit public-observer integration batch.
